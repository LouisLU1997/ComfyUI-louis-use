# ComfyUI 自带，通常无需单独安装
# torch>=2.0.0
# Pillow>=9.0.0
# numpy>=1.24.0
# aiohttp>=3.8.0

# Text Segmenter / Text Segmented Depth 所需
transformers>=4.30.0
huggingface_hub>=0.16.0
torchvision>=0.15.0
timm>=1.0.0  # BiRefNet 远程代码需要 timm.layers（timm>=0.9）
